// TODO(Document 9, Phase 2 — Database): seed script — depends on the
// domain models (Project, Knowledge, ...) that Phase 2 introduces.
// Intentionally a no-op in Phase 0/1.

async function main(): Promise<void> {
  console.log("Nothing to seed yet — see Document 9, Phase 2.");
}

main();
