// TODO(Document 9, Phase 2 — Database): migration helper/wrapper around
// `prisma migrate`. Intentionally a no-op in Phase 0/1 — use
// `npm run db:migrate` (Prisma CLI directly) until this exists.

export {};
